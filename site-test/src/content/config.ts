// Static data files — imported directly via JSON imports
// Content collections will be configured via src/content.config.ts when using Astro collections
// Currently using direct JSON imports (faq.json, process.json) — no Astro collections needed
