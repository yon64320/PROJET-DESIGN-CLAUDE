import { defineCollection } from 'astro:content'

// No collections defined — data is loaded via static JSON imports in src/content/
// Add collections here when integrating Sanity CMS (loader pattern)
export const collections = {}
