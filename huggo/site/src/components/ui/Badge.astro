---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span class={`inline-flex items-center px-3 py-1 bg-accent-subtle text-accent-action text-body-sm font-semibold rounded-sm ${className}`}>
  <slot />
</span>
